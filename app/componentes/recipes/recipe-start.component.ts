import { Component } from '@angular/core';

@Component({
  moduleId: module.id,
  selector: 'recipeStart',
  template: `<h1>Select a recipe</h1>`,
})
export class RecipeStartComponent { 
  
}